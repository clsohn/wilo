<ul class="slideshow">
  <li><span>Image 01</span></li>
  <li><span>Image 02</span></li>
  <li><span>Image 03</span></li>
  <li><span>Image 04</span></li>
  <li><span>Image 05</span></li>
  <li><span>Image 06</span></li>
</ul>

<div class="container">
  <div class="top-bar">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <div class="top-bar-right">
        <ul class="menu horizontal">
          <% if user_signed_in? %>
            <li>
              <%= link_to "Hi #{current_user.first_name}",
                user_path(current_user)
              %>
            </li>
            <li>
              <%= link_to "Sign Out",
                destroy_user_session_path, method: :delete
              %>
            </li>
          <% else %>
            <li> <%= link_to "Sign In", new_user_session_path %> </li>
            <li> <%= link_to "Sign Up", new_user_registration_path %> </li>
          <% end %>
        </ul>
    </div>
  </div>
  <div class="title">What's it like out?</div>
    <% if user_signed_in? && @location != nil %>
    <h2> See today's <span><%= link_to "recommendation", new_user_path %></span> for <%= current_user.first_name %></h2>
      <div class="jumbotron">
        <div class="box">
          <div class="blockhead">
            <h3>Your current location</h3>
          </div>
          <div class="blockbody">
            <h2><%= @location.city %> <%= @location.state %></h2>
          </div>
        </div>
        <h2>Going on a trip? Change your <span><%= link_to "location!", new_location_path %></span></h2>
      </div>
    <% else %>
      <h2>First time here? Enter your <span><%= link_to "Profile Info", new_location_path  %></span> for your personal recommendation</h2>
    <% end %>
    <h4><%= link_to "Change your email or password", edit_user_registration_path %></h4>
  </div>
